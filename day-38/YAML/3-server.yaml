server:
  name: devops-server
  ip: 192.168.1.100
  port: 8080

database:
  host: localhost
  name: devops_db
  credentials:
    user: devops_user
    password: change_me

startup_script: |
  #!/bin/bash
  echo "Starting DevOps server"
  systemctl start nginx
  echo "Server started"
